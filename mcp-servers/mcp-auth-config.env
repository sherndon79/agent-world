# MCP Servers Authentication Configuration
# This file configures authentication settings for all MCP servers

# Global authentication settings
# Set to 0 to disable auth for all servers, 1 to enable (default)
AGENT_EXT_AUTH_ENABLED=1

# Global HMAC secret and auth token (used by servers that support auth)
# Generate a secure random secret for production use
AGENT_EXT_HMAC_SECRET=8f1d59a79a882bfecabead9aaae247e3644361089beb8730f5c5fe6da26b1013a5eb3cf51ef0305a4d03deb51ca97a3c
AGENT_EXT_AUTH_TOKEN=444e075cefddd542a668a048be558bdea0b8cbc70226002fef3ecdaed7bfbb62

# Server-specific overrides
# These servers support HMAC authentication and should use it:

# WorldBuilder server (working with auth)
AGENT_WORLDBUILDER_HMAC_SECRET=${AGENT_EXT_HMAC_SECRET}
AGENT_WORLDBUILDER_AUTH_TOKEN=${AGENT_EXT_AUTH_TOKEN}

# WorldRecorder server (working with auth)  
AGENT_WORLDRECORDER_HMAC_SECRET=${AGENT_EXT_HMAC_SECRET}
AGENT_WORLDRECORDER_AUTH_TOKEN=${AGENT_EXT_AUTH_TOKEN}

# WorldViewer server (working with auth)
AGENT_WORLDVIEWER_HMAC_SECRET=${AGENT_EXT_HMAC_SECRET}
AGENT_WORLDVIEWER_AUTH_TOKEN=${AGENT_EXT_AUTH_TOKEN}

# VideoCapture server - DISABLE AUTH (Isaac Sim extension doesn't have auth configured)
# By not setting these variables, auth will be disabled for this server only
# AGENT_VIDEOCAPTURE_HMAC_SECRET=  # Leave unset to disable
# AGENT_VIDEOCAPTURE_AUTH_TOKEN=   # Leave unset to disable

# WorldSurveyor server - No auth implementation yet
# This server doesn't implement authentication yet, so no variables needed

# Screenshot server - Check if it needs auth configuration
# AGENT_SCREENSHOT_HMAC_SECRET=${AGENT_EXT_HMAC_SECRET}
# AGENT_SCREENSHOT_AUTH_TOKEN=${AGENT_EXT_AUTH_TOKEN}